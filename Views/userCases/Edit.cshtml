@model ThesisApplication.Models.userCase

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<form asp-action="Edit">
    <div class="form-horizontal">
        <h4>userCase</h4>
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="ID" />

        <!-- ADD MORE HIDDEN FIELDS!! -->
        @Html.Partial("/Views/Shared/_partialMeshingHiddenFields.cshtml")
        @Html.Partial("/Views/Shared/_partialGeneralHiddenFields.cshtml")
        
        <h5>For now, nothing is modifiable!</h5>
        <h5> However future implementations are possible!</h5>
        <!--
        <div class="form-group">
            <label asp-for="unitModel" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="unitModel" class="form-control" />
                <span asp-validation-for="unitModel" class="text-danger" />
            </div>
        </div>-->

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
